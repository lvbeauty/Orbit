# OS / editor
.DS_Store
.vscode/
.idea/

# backend (Node/TypeScript)
backend/node_modules/
backend/dist/
backend/.env
backend/uploads/*
!backend/uploads/.gitkeep

# app/ (Flutter) carries its own .gitignore for build/, .dart_tool/, and the ios/android
# build artifacts that used to live at the repo root back when this was a plain Xcode project.
